export default function KisKep(props) {
    /*
    function kattintas(adat) {
        kattKicsi(adat)
    }
    */

    return
    (
        <div className="elem">
            <img src={`${props.kep.src}`} alt={`${props.kep.alt}`} />
        </div>
    )
}